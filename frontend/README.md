Pool Hall POS Frontend (Prototype)

Requirements
- Node.js 18+ recommended

Quick start

```bash
cd frontend
npm install
npm run dev
```

This prototype includes a minimal dashboard (table map) and checkout page. It uses mocked WebSocket events for demo; backend integration will be added in next steps.